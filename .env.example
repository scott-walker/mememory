# Directory for persistent data (Qdrant vectors + Ollama models)
# Default: ~/.claude-memory
MEMORY_DATA_DIR=~/.claude-memory

# Port mappings (change if defaults conflict with other services)
QDRANT_PORT_REST=6333
QDRANT_PORT_GRPC=6334
OLLAMA_PORT=11434
ADMIN_PORT=4200
