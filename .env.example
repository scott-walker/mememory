# Directory for persistent data (PostgreSQL + Ollama models)
# Default: ~/.mememory
MEMORY_DATA_DIR=~/.mememory

# Port mappings (change if defaults conflict with other services)
POSTGRES_PORT=5432
POSTGRES_PASSWORD=memory
OLLAMA_PORT=11434
ADMIN_PORT=4200
